'use strict';

/**
 * Class CertInfo
 *
 * notBefore and notAfter are the number of milliseconds since the UNIX Epoch ("UNIX time" in ms).
 *
 * @property {string} notBefore - Start of validity of the users BankID. Unix ms.
 * @property {string} notAfter  - End of validity of the users BankID. Unix ms.
 */
class CertInfo {}

module.exports = CertInfo;
